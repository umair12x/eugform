export const metadata = {
  title: "Manager Portal | UAF Digital Enrollment Portal",
  description:
    "Manager access to review and manage student enrollment forms. Approve, request revisions, and oversee the enrollment process.",
};

export default function managerLayout({ children }) {
  return <>{children}</>;
}
