export const metadata = {
  title: "Fee Office Portal | UAF Digital Enrollment Portal",
  description: "Fee office access to verify student fee payments.",
};

export default function feeOfficeLayout({ children }) {
  return <>{children}</>;
}
